# Dependencies
node_modules/
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config (client-side config — generated from env vars)
public/scripts/firebase-config.js
web/static/scripts/firebase-config.js
web/ts/shared/firebase-config.ts

# Firebase service account (sensitive)
firebase-service-account.json

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Environment files
.env
.env.local
.env.*.local

# IDE/Editor specific
.vscode/
.idea/
.windsurf/
*.swp
*.swo
.DS_Store

# Task runner artifacts
.task/

# Go build artifacts
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go test cache
*.test
*.out
coverage.html

# TypeScript / esbuild build output
web/static/dist/

# Docker volumes
pgdata/

# Build/Deploy
dist/
build/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Browser specific
.cache/
.temp/
